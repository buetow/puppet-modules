To approve requests:

./list_requests --verbose --list=friends

=> Note request number (e.g. 45)

./withlist -l friends
Loading list friends (locked)
>>> m.HandleRequest(45,1)
>>> m.Save()
>>>
Unlocking (but not saving) list: friends Finalizing

