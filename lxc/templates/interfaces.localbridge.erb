## LXC local bridge start (Don't manually edit from this line)

# Pseudo device, for local bridge 
auto tap0
iface tap0 inet manual
  pre-up tunctl -u root -t tap0
  tunctl_user root

# Local bridge, always has 192.168.53.1/24, doesn't matter which LXC host
auto br0
iface br0 inet static
  address 192.168.53.1
  netmask 255.255.255.0
  bridge_ports tap0
  bridge_stp off

## LXC local bridge stop
