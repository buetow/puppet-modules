# This file is managed by Puppet. All changes will be reverted
# on the next Puppet run. Avoid making changes here.
<%# We can prevent excess newlines by using a special "-%" closing tag -%>
<% @entries.each_pair do |ip,hosts| -%>
<%= ip %> <%= hosts %>
<% end -%>
